
export interface LogEntry {
  id: string;
  date: string;
  title: string;
  content: string;
  coordinates: string;
  weather: string;
  author: string;
}

export interface NavigationLink {
  label: string;
  href: string;
}
